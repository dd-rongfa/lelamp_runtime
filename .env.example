# 复制为 .env，填你自己的火山豆包凭据（.env 已被 .gitignore 排除，不会进仓库）。
#   cp .env.example .env
# 凭据都在火山引擎控制台同一账号下，分两类 key：语音一把、方舟 Ark 一把。

# —— 语音 STT/TTS（必填）——
# 火山控制台 → 语音技术 → 大模型语音，开通后拿「新版 v3 单 X-Api-Key」。
VOLCENGINE_VOICE_API_KEY=

# —— LLM + 视觉（必填）——
# 火山控制台 → 方舟(Ark) → API Key。默认用全模态 Doubao-Seed-2.0-lite（聊天+看图一颗脑）。
LLM_API_KEY=
# 可选覆盖：
# LLM_MODEL=doubao-seed-2-0-lite-260428
# LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3/
# LLM_THINKING=disabled            # 豆包 Seed 默认开思考 TTFT 7~12s，语音务必 disabled
# LAMP_SPEAKER=zh_female_vv_uranus_bigtts   # 默认女声 Vivi 2.0

# —— 视觉 look ——
# 想看实时画面（推荐）：先装摄像头库 `uv sync --extra vision`，有摄像头就**自动优先用**。
# 下面这张是「没摄像头时的兜底测试图」（仓库自带，开箱可验证视觉链路）；
# 有摄像头时它会被忽略——摄像头优先。纯无摄像头 demo 才靠它。
LAMP_VISION_IMAGE=assets/images/1_lamp_3d.png
# LAMP_CAMERA_INDEX=0        # 多摄像头时选序号

# —— 无硬件运行（Windows/Mac/Ubuntu 没接机械臂/灯时）——
# 置 1：电机/LED 走 mock，只打日志、不碰串口/GPIO。
# 部署到接了硬件的树莓派/真机时，删掉此行或设 0。
LELAMP_NO_HARDWARE=1
